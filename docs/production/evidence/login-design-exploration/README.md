# Login Design Exploration — Evidence

`preview_screenshot` timed out on every attempt during this exploration
(see the "Screenshot evidence" section of
[`LOGIN_DESIGN_EXPLORATION.md`](../../LOGIN_DESIGN_EXPLORATION.md)), so no
screenshot image files were exported here. The certification evidence —
DOM measurements (card/column width and height at 390×844 and 1440×900,
overflow checks) and an accessibility-tree snapshot per concept — is
recorded in that document's evidence table instead.
