# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — never commit real secrets.
# .env.example is intentionally NOT ignored (names only, no values).
.env
.env.local
.env.production
.env.development
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify

# Local render/capture scratch artifacts (login background compositing)
.render-tmp/

# Local-only pre-mutation data snapshots (never commit — may contain full document content)
local-backups/
